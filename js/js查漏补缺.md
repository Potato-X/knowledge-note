# 正则捕获提取
```
let reg = new RegExp(/\((.*)\)/);
let res = reg.exec('(223,100,20,1)') //作用是捕获括号里面的所有内容,正则语法里面有个()这样的小括号，有别于我们用'\('进行转义的括号，直接写括号表示要捕获括号里面的内容，通过match或者exec获取匹配结构,从获取的结果数组里面第二个数据得到目标值
console.log(res[1]) //打印出=>223,100,20,1
```